import SwiftUI

extension Color {
    // Primary accent red color
    static let accentRed = Color(red: 0.8, green: 0.0, blue: 0.0)
    
    // You can also define variations if needed
    static let accentRedLight = Color(red: 0.9, green: 0.1, blue: 0.1)
    static let accentRedDark = Color(red: 0.6, green: 0.0, blue: 0.0)
}